// Provides shared configuration constants across the application
const API_BASE_URL = '../php/'; // Defines the relative path to the PHP API endpoints
const SESSION_CHECK_URL = '../php/login.php?action=session'; // Points to the session validation endpoint used on protected pages
